#include <stdio.h>

int main(int argc, char const *argv[])
{
    // int score = 4567890;

    // int every_other_number[3];

    // for (int i = 0; i < 3; i++)
    // {
    //     score /= 10;
    //     every_other_number[i] = score % 10;
    //     score /= 10;
        
    // }

    // for (int i = 0; i < 3; i++)
    // {
    //     printf("%i\n", every_other_number[i]);
    // }

    int score = 50;
    int scores = score / 25;

    printf("%i\n", scores);

    return 0;
}
